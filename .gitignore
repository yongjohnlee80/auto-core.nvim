*.swp
*.swo
*.luac
.DS_Store
.luarc.local.json
.tests/

# nvim runtime log — written by `nvim --headless` smoke runs in this repo,
# environment-specific, not part of the release artifact.
nvim.log
# Per-project CLAUDE.md is auto-injected by auto-agents into the agent's
# cwd. When the plugin is developed in-place inside this repo, that
# CLAUDE.md lands here — environment-specific, not part of the release.
CLAUDE.md

# auto-agents workspace mailbox (v0.2.30+)
.auto-agents/
